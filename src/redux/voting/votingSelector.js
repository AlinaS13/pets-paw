export const getVoting = (state) => state.voting.voting;

export const getAllVotes = (state) => state.voting.votes;
export const getAllFavourites = (state) => state.voting.favouritesCat;

export const isLoadingLikes = (state) => state.voting.isLoading;
